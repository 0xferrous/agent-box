<!-- AUTO-GENERATED by docs/scripts/generate-cli-reference.nu -->

_Do not edit by hand. Regenerate with:_

```bash
nu docs/scripts/generate-cli-reference.nu
```

# Portal CLI reference
## `agent-portal-host`

Command:

```bash
cargo run -q -p agent-portal --bin agent-portal-host -- --help
```

Output:

```text
Host portal service for container capability requests

Usage: agent-portal-host [OPTIONS]

Options:
      --socket <SOCKET>  Override socket path
  -h, --help             Print help
```

## `agent-portal-cli`

Command:

```bash
cargo run -q -p agent-portal --bin agent-portal-cli -- --help
```

Output:

```text
Official CLI client for agent portal host service

Usage: agent-portal-cli [OPTIONS] <COMMAND>

Commands:
  ping                  
  whoami                
  clipboard-read-image  
  gh-exec               
  help                  Print this message or the help of the given subcommand(s)

Options:
      --socket <SOCKET>  Override socket path
  -h, --help             Print help
```

## `agent-portal-client`

Command:

```bash
cargo run -q -p agent-wrappers --bin agent-portal-client -- --help
```

Output:

```text
Lightweight portal client for wrappers/scripts

Usage: agent-portal-client [OPTIONS] <COMMAND>

Commands:
  list-types  
  read-image  
  help        Print this message or the help of the given subcommand(s)

Options:
      --socket <SOCKET>  
  -h, --help             Print help
```

## `wl-paste wrapper`

Command:

```bash
cargo run -q -p agent-wrappers --bin wl-paste -- --help
```

Output:

```text
Usage: wl-paste [--list-types] [--type <mime>] [--no-newline]
```
